var name = 'Juan', lastname = 'Gonzalez';

var nameincapitalletters = name.toUpperCase();
var lastnameinlowercase = lastname.toLowerCase();

var firstletterfromname = name.charAt(0);
var quantityfromletterofthename = name.length;

var fullname = `${name} ${lastname.toUpperCase()}`

var str = name.substr(2);